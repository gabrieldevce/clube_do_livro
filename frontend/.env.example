# Variável na Vercel (Settings → Environment Variables):
# NEXT_PUBLIC_API_URL = https://clube-do-livro-luwb.onrender.com
# (já está como fallback no next.config; defina na Vercel para sobrescrever se precisar)
